services:
  - type: web
    name: collectwise-account-lookup
    env: node
    buildCommand: npm install
    startCommand: npm run ingest && npm start
    healthCheckPath: /health
    autoDeploy: false
